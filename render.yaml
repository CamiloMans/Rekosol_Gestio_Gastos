services:
  - type: web
    name: rekosol-gestion-gastos
    env: node
    rootDir: .
    buildCommand: npm install && npm run build
    startCommand: npm run server
    pullRequestPreviewsEnabled: true
    headers:
      - path: /*
        name: X-Robots-Tag
        value: noindex

